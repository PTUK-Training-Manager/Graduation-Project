import React, { FC } from 'react';
import Box from '@mui/material/Box';

interface AcceptedRequestsProps {}

const AcceptedRequests: FC<AcceptedRequestsProps> = (props) => {

  return (
    <Box>AcceptedRequests</Box>
  );
};

export default AcceptedRequests;
