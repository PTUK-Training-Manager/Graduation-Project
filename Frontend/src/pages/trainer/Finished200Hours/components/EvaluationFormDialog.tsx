import React, { FC } from 'react';

interface EvaluationFormDialogProps {}

const EvaluationFormDialog: FC<EvaluationFormDialogProps> = (props) => {
  
  return (
    <div>EvaluationFormDialog</div>
  );
};

export default EvaluationFormDialog;
